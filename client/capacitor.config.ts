import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.aigolfcaddie.app',
  appName: 'AI Golf Caddie',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
